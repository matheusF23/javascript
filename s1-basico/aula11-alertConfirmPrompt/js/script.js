//alert('Com a nossa mensagm')

/* No console do navegador, utilizar os metodos
 *
 * window.alert('texto');
 * window.confirm('texto');
 * window.prompt('texto');
 * 
 * Pode ser usado sem o window. também. apenas com os atalhos alert, confirm e prompt
 */

let num1 = Number(prompt('Digite um número'));
let num2 = Number(prompt('Digite outro número'));
alert(`A soma dos dois números é ${num1 + num2}`);

// Valor por referência

// const nomes = ['Eduardo', 'Maria', 'joana'];
// delete nomes[2]; // Remove um objeto do array sem remover o indice

// const nomes = new Array('Eduardo', 'Maria', 'joana'); // rambem pode ser criado assim
// console.log(nomes);

// const novo = [...nomes];  // forma de copiar os dados do array nomes para um novo array
                          // pq como o array pe um valor por referência fazendo apenas
                          // novo = nomes, as duas variáveis estarão apontando para o mesmo array
// console.log(novo);

// const novo = nomes.slice(1, -1); // cria um novo array com um intervalo do array nomes
// console.log(novo);
